import styled from "@emotion/styled";

export const PpayProLogoWrapper = styled.div`
  font-size: 2rem;
`;

export const OutlinedText = styled.span`
  padding: 8px 16px;
  border: 2px solid var(--white-inno);
  border-radius: var(--border-radius-small);
`;
